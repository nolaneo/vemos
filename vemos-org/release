cd ..
mkdir site
cp vemos-org/index.html
cp CNAME ./site
git add site
git commit -am "site release"
git subtree split --prefix site -b gh-pages
git push -f origin gh-pages:gh-pages
git branch -D gh-pages
git reset HEAD --hard